import Vue from 'vue';

/*const navigation = new Vue({
    el: '.arrow',
    methods: {
        goTo: (place) => {
            if(place === 'skills'){
                $('.sidebar').animate()
            }
        }
    }
});*/
